{
  "name": "claudey-admin-beam",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Isolated React micro-frontend for the provider-beam diagram. Built with esbuild into admin_static/beam.bundle.js; the bundle is committed and served offline from the admin UI.",
  "scripts": {
    "build": "node build.mjs"
  },
  "dependencies": {
    "clsx": "^2.1.1",
    "motion": "^12.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "tailwind-merge": "^3.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.6.0"
  }
}
